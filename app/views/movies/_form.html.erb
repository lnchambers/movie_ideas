<%= form_for [@movie, @image] do |f| %>

<%= f.label :title %>
<%= f.text_field :title %>

<%= f.label :description %>
<%= f.text_field :description %>

<%= f.label :rating %>
<%= f.hidden_field :rating, :value => 0 %>

<%= f.label :image %>
<%= f.text_field :image %>

<%= f.submit %>

<% end %>
